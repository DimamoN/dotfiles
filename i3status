general {
        colors = true
        interval = 5
        color_good = "#2AA198"
        color_bad = "#586E75"
        color_degraded = "#DC322F"
}

order += "cpu_usage"
order += "tztime local"

cpu_usage {
  format = " CPU: %usage "
}

tztime local {
  format = "%Y-%m-%d %H:%M"
}
